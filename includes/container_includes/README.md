This directory is included in our Docker images and can be thought of as a means to package
semi-static configurations that the app needs, that merit version control, that can also
be overridden in the case of emergency (eg. with k8s volume mounts).
