[package]
name = "floword-launcher"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
zip = { version = "2.2", default-features = false, features = ["deflate"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
