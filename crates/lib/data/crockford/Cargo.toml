[package]
name = "crockford"
edition = "2018"
version = "0.0.1"
authors = [
    "Brandon Thomas <bt@brand.io>",
    "Brandon Thomas <echelon@gmail.com>",
]
publish = false

[lib]
# NB: Public "crockford" crate is non-sensically only for encoding and decoding u64s (wtf?)
# Our crate is for generating entropy and crockford encoding it.
name = "crockford"
path = "src/lib.rs"

[package.metadata.cargo-udeps.ignore]
normal = ["workspace-build-acceleration"]

[dependencies]
# Internal
# (None yet)

# Workspace hack (faster compile times)
workspace-build-acceleration = { version = "0.1", path = "../../../workspace-build-acceleration" }

# External
rand = "0.8.5"

[dev-dependencies]
# None yet
